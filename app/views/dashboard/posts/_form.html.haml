= simple_form_for [ :dashboard, post ] do |f|
  = f.input :title
  %ul.nav.nav-tabs#content-tabs
    %li.active= link_to t('posts.editor'), '#editor', :data => {:toggle => :tab}
    %li= link_to t('posts.preview'), '#preview', :data => {:toggle => :tab}

  .tab-content
    .tab-pane.active#editor
      .wmd-panel
        #wmd-button-bar
        = f.input :content, :label => false, :input_html => { :id => 'wmd-input', :class => 'wmd-input' }
    .tab-pane#preview
      .wmd-panel
        #wmd-preview

  = f.input :excerpt, :as => :text, :input_html => { :class => 'excerpt' }
  = f.input :tag_list, :hint =>  t('hints.tags')
  = f.input :published, :hint => t('hints.published')

  .form-actions
    = f.button :submit, :class => 'btn-success'
