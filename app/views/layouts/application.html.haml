!!!
%html
  %head
    %title RubyTij - User Group
    / [if lt IE 9]
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>

    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = yield :head
    = csrf_meta_tags

    %meta{ :name => :viewport, :content => 'width=device-width, initial-scale=1.0' }
  %body
    = render 'layouts/navigation'
    .wrapper
      %header
        .logo
        %nav#site_sections
          %ul
            - symbolize = 'activerecord.symbolizes.page.section'
            - [ Page.home.first, Page.about.first, Page.contact.first, Page.meetings.first ].compact.each do |page|
              %li= link_to t("#{ symbolize }.#{ page.section }"), section_page_path( page.section, page )
      - if @page.try(:main_page?)
        %section.banner
          .photos
          .banner_info
            %h1 Somos rubytij
            %p Sitio de informacion sobre el lenguaje de programacion Ruby.
          %section.quick_news
      %section.main
        .main_content
          = yield
        .latest_articles
      %section.ruby_tij_network_sites
        .github.widget
          = image_tag 'github_logo.png'
        .reddit.widget
          = image_tag 'reddit_logo.png'
        .twitter.widget
          = image_tag 'twitter_logo.png'
      %footer
        %p CC rubytij
