!!!
%html
  %head
    %title RubyTij - User Group
    / [if lt IE 9]
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>

    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = yield :head
    = csrf_meta_tags

    %meta{ :name => :viewport, :content => 'width=device-width, initial-scale=1.0' }
  %body
    = render 'layouts/navigation'
    .wrapper
      %header
        = link_to '', '/', :class => 'logo'
        %nav#site_sections
          %ul
            - symbolize = 'activerecord.symbolizes.page.section'
            - [ Page.home.first, Page.about.first, Page.meetings.first ].compact.each do |page|
              %li= link_to t("#{ symbolize }.#{ page.section }"), section_page_path( page.section, page )
      - if @page.try(:main_page?)
        %section.banner
          .photos
          .banner_info
            %h1 Somos rubytij
            %p Sitio de informacion sobre el lenguaje de programacion Ruby.
      %section.quick_news
      %section.main
        .main_content
          = yield
        .additional_content
          = yield :additional_content
      %section.ruby_tij_network_sites
        .github.widget
          %a{ :href => '//github.com/rubytij' }
            = image_tag 'github_logo.png'
        .reddit.widget
          %a{ :href => '//reddit.com/r/rubytij' }
            = image_tag 'reddit_logo.png'
        .twitter.widget
          %a{ :href => '//twitter.com/rubytij' }
            = image_tag 'twitter_logo.png'
      %footer
        %p CC rubytij
