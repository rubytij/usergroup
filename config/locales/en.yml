en:
  user:
    account: 'Account'
    registrations:
      show: 'Profile'
    sessions:
      sign_in: 'Sign in with GitHub'
      destroy: 'Sign out'

  actions:
    new: New %{item}

  hints:
    tags: Separate tags by commas.

  activerecord:
    models:
    attributes:
      user:
        github_uid: UID
        site_url: Blog
